# Frontend environment for local development (Vite).
# Copy to .env and adjust per environment. Only VITE_* variables are
# exposed to client code via import.meta.env; deployment builds inject
# the value at build time.
VITE_API_BASE_URL=http://localhost:8001
